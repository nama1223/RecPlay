# RecPlay / Nama* シリーズ 引き継ぎメモ

> 作成日: 2026-05-21
> 最終更新: 2026-05-21（PWA対応を追加）
> Claude Code セッション終了時点の情報

---

## 1. プロジェクト一覧とローカルパス

| プロジェクト | ローカルパス | GitHub | 配信URL |
|---|---|---|---|
| **RecPlay**（録音共有） | `C:\Users\nama1\Desktop\出力ファイル\FFFTP\RecPlay` | `https://github.com/nama1223/RecPlay` | `https://nama1223.github.io/RecPlay/` |
---

## 2. 外部サービスと接続方法

### Cloudflare（RecPlay のバックエンド）

#### Workers
- Worker名: `recplay`
- URL: `https://recplay.nama1223.workers.dev`
- ソース: `src/index.js`（単一ファイル、バンドル不要なプレーンJS）
- デプロイ: `npx wrangler deploy`（設定ファイル: `wrangler.jsonc`）

#### R2（音声ファイルストレージ）
- バケット名: `recording-player`
- アカウントID: `4600f3a7aa72ec4fa62b3a9a24afb2b8`（`wrangler.jsonc` に記載）
- Workerからのバインディング名: `env.recplay_audio`
- R2内のキー構造:
  - 音声ファイル本体: `{orgId}/{timestamp}_{filename}.mp3`
  - 区間データ: `sections/{orgId}/{timestamp}_{filename}.mp3.json`
  - 波形データ: `waveforms/{orgId}/{timestamp}_{filename}.mp3.json`

#### KV（団体情報ストレージ）
- バインディング名: `RECPLAY_KV`
- KV ID: `5410a23ebfdc46488258f6decb119b24`
- 保存内容: `orgs` キーに JSON 配列 `[{ id, name, password }, ...]`

#### Workerのシークレット（値は Cloudflare ダッシュボードでのみ確認・変更可能）
- `R2_ACCESS_KEY_ID` — R2 S3互換APIのアクセスキー
- `R2_SECRET_ACCESS_KEY` — 同シークレットキー
- `ADMIN_PASSWORD` — 管理者パネル用パスワード

変更する場合: `npx wrangler secret put <キー名>`

### GitHub Pages（フロントエンド配信）

| プロジェクト | 配信方式 | 初回設定 |
|---|---|---|
| RecPlay | `main` ブランチへ push → 自動配信 | 不要（設定済み） |

---

## 3. RecPlay のファイル構成

```
RecPlay/
├── wrangler.jsonc              ← Cloudflare Worker設定（R2・KVバインディング定義）
├── package.json                ← React18 + Vite5 + TypeScript、@breezystack/lamejs
├── src/
│   ├── index.js                ← Cloudflare Worker本体（APIサーバー）
│   ├── config.ts               ← WORKER_URL / R2_PUBLIC_URL / URLビルダー関数
│   ├── App.tsx                 ← 画面遷移管理（auth → files → player / admin）
│   ├── App.css / index.css / main.tsx
│   ├── types/
│   │   └── index.ts            ← Section / AppMode / ZoomLevel / AppSettings 型定義
│   ├── hooks/
│   │   ├── useAudioPlayer.ts   ← 再生制御・除外ゾーンスキップ・速度変更
│   │   ├── useSections.ts      ← 区間CRUD・undo/redo・並べ替え
│   │   ├── useSectionSync.ts   ← R2との区間データ自動同期
│   │   ├── useSeekBar.ts       ← ズームレベル管理
│   │   ├── useSettings.ts      ← LocalStorage設定管理
│   │   ├── useWaveform.ts      ← 波形データのR2キャッシュ管理
│   │   └── useOrgAuth.ts       ← 団体情報のLocalStorage管理
│   ├── components/
│   │   ├── AuthPage.tsx        ← パスワード認証・団体選択
│   │   ├── FileListPage.tsx    ← ファイル一覧・アップロード・削除・リネーム・複製
│   │   ├── AdminPage.tsx       ← 団体管理（管理者パスワード必要）
│   │   ├── ModeSelector.tsx    ← play/edit/settings モード切替
│   │   ├── SettingsPanel.tsx / FileLoader.tsx
│   │   ├── SeekBar/
│   │   │   ├── SeekBar.tsx     ← ズーム管理・旗ドラッグの中心
│   │   │   └── SeekBarRow.tsx  ← 1段の描画（LABEL_WIDTH=64, ROW_HEIGHT=52）
│   │   ├── Sections/
│   │   │   ├── SectionList.tsx / SectionItem.tsx / FlagEditor.tsx
│   │   └── Controls/
│   │       └── PlaybackControls.tsx
│   └── utils/
│       ├── mp3Download.ts      ← バイナリRange取得＋ダウンロード
│       ├── r2Upload.ts         ← 署名付きURL経由でR2へ直接アップロード
│       ├── normalize.ts        ← 音量正規化（Web Audio API + lamejs 再エンコード）
│       ├── waveformCompute.ts  ← 波形データ生成
│       └── timeFormat.ts
```

---

## 4. Worker APIエンドポイント一覧

| メソッド | パス | 概要 |
|---|---|---|
| POST | `/presign` | R2への署名付きPUT URLを発行（アップロード前に呼ぶ） |
| PUT | `/upload?key=` | R2バインディング経由で直接保存（小ファイル用フォールバック） |
| GET | `/audio?key=` | 音声ファイルをRange対応で配信 |
| HEAD | `/audio?key=` | ファイルサイズ取得 |
| GET | `/files?org=` | 団体のファイル一覧取得 |
| POST | `/files/copy` | ファイル複製（音声＋区間＋波形） |
| DELETE | `/files?key=` | ファイル削除（音声＋区間＋波形） |
| PATCH | `/files` | ファイルリネーム `{ key, newName }` |
| GET | `/sections?file=` | 区間データ取得 |
| PUT | `/sections?file=` | 区間データ保存 |
| GET | `/waveform?file=` | 波形データ取得 |
| PUT | `/waveform?file=` | 波形データ保存 |
| GET | `/orgs` | 団体一覧取得（パスワードなし） |
| POST | `/auth` | パスワードで団体認証 |
| GET | `/org?id=` | 団体名取得（URL直リンク自動ログイン用） |
| GET | `/ping` | 診断用 |
| GET/POST/PUT/DELETE | `/admin/orgs[/{id}]` | 団体管理（要 Authorization ヘッダー） |
| DELETE | `/admin/files/{key}` | 管理者によるファイル強制削除 |

管理者エンドポイントはすべて `Authorization: Bearer {ADMIN_PASSWORD}` ヘッダーが必要。

---

## 5. RecPlay アプリ固有の注意点・落とし穴

### Section 型（最重要）

```typescript
interface Section {
  id: string
  label: string      // ← "name" ではなく "label"！ 間違えやすい
  startTime: number
  endTime: number
  isExcluded: boolean
  color: string      // ← 省略不可。SECTION_COLORS の値から選ぶ
}

// SECTION_COLORS (types/index.ts に定義)
const SECTION_COLORS = [
  '#4A90E2', '#7ED321', '#F5A623', '#9013FE',
  '#50E3C2', '#E25C4A', '#E24AB0', '#4AE2B5',
]
```

### 区間データの R2 同期（useSectionSync）
- **編集モード（`mode === 'edit'`）でのみ** 変更を R2 へ push
- 再生モードでは他ユーザーの変更を 30 秒ごとに pull
- Python など外部から `/sections?file={key}` を fetch する際は **`User-Agent` ヘッダーが必須**（なければ 403 になる）

### LocalStorage のキー
- `recplay_orgs` — ログイン済み団体の id / name
- `recplay-own-keys` — このブラウザでアップロードしたファイルキー（削除ボタンの表示判定に使用）
- `recplay-settings` — スキップ秒数などの設定

### ファイルキーの構造
- 形式: `{orgId}/{timestamp}_{filename}`
- 例: `org_1716000000000/1716001234567_practice.mp3`
- リネーム時: タイムスタンプ部分は変わらず、ファイル名部分だけ変わる
- 表示名: キーから `{orgId}/` とタイムスタンプ `_` プレフィックスを除いた部分

### 音声配信の仕組み
- 音声は **Worker の `/audio?key=` 経由**で配信（R2 の公開 URL は使わない）
- Range request に対応（1 チャンク最大 2MB）
- `buildWorkerAudioUrl(key)` → `{WORKER_URL}/audio?key={encodeURIComponent(key)}`

### アップロードの仕組み
1. ブラウザ → Worker `/presign` で署名付き PUT URL を取得
2. ブラウザ → R2 へ直接 PUT（Worker を経由しない）

### 管理者ページへのアクセス
- URL末尾に `?admin` を付けると管理者ページへ遷移（`AuthPage.tsx` 内で判定）
- 管理者パスワードは Cloudflare ダッシュボード > Workers > recplay > Settings > Variables で確認・変更

### 開発コマンド
```bash
# フロントエンド開発
npm run dev          # Vite 開発サーバー（localhost:5173）
npm run build        # ビルド → dist/

# Worker の開発・デプロイ
npx wrangler dev     # ローカル Worker サーバー
npx wrangler deploy  # 本番デプロイ（src/index.js を Cloudflare へ）
```

---

## 9. PWA対応（2026-05-21 追加）

RecPlay は PWA（Progressive Web App）対応済み。ホーム画面に追加してオフライン対応のネイティブアプリのように使える。

### 構成
- ライブラリ: `vite-plugin-pwa` v1.3.0（devDependency）
- 設定: `vite.config.ts` の `VitePWA()` プラグイン
- アイコン: `public/NamaRecPlay192.png` / `512.png` / `1024.png`

### 自動更新の仕組み
1. アプリ初回アクセス時にService Worker（SW）が登録・インストールされ、全アセットをキャッシュ
2. 次回以降はキャッシュから即時表示（高速・オフライン動作）
3. デプロイ後にブラウザがSWの更新を検出すると:
   - 新SWが即座に有効化（`skipWaiting: true`）
   - 新SWが全クライアントを掌握（`clientsClaim: true`）
   - `controllerchange` イベント → `location.reload()` でページ自動リロード

### アイコンについて
- マニフェストで `"purpose": "any"` を指定（OSによる角丸適用なし）
- iOS: `<link rel="apple-touch-icon">` でホーム画面アイコンに使用
- Android/Chrome: マニフェストのアイコンを使用

### 修正時の注意
- `vite.config.ts` の `manifest` セクションを直接編集すればマニフェスト内容を変更できる
- アイコンを変えたい場合は `public/` 内の PNG を差し替えてビルドし直すだけ
- `lang` フィールドがデフォルト `"en"` になってしまう場合は `manifest` に `lang: 'ja'` を追加する

---

## 11. 通常の Claude に依頼する際のコツ

1. **修正したいファイルの内容をそのまま貼り付ける**（Claude Code と違い、ファイルを直接読めないため）
2. **Worker API を変更した場合は `npx wrangler deploy` が別途必要**（フロントエンドのビルドとは独立）
3. **Section の `label` フィールド**を `name` と書かないよう念押しすると安全
4. **区間データを直接 API で操作する場合は `User-Agent` ヘッダーを付ける**
